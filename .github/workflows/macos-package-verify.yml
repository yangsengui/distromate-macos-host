name: Verify distromate package (macOS)

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: Release tag that contains distromate-darwin-* binaries
        required: true
        default: macos-local-f097f57-20260211063432
      runner:
        description: macOS runner version
        required: true
        default: macos-14
        type: choice
        options:
          - macos-14
          - macos-15
          - macos-13
      binary_arch:
        description: distromate binary arch (auto uses runner arch)
        required: true
        default: auto
        type: choice
        options:
          - auto
          - amd64
          - arm64
      app_version:
        description: Test app version used in package command
        required: true
        default: 0.0.1
      debug_enabled:
        description: Start tmate session after failure
        type: boolean
        required: false
        default: false
      production_signature_checks:
        description: Enable production-grade signature trust checks
        type: boolean
        required: false
        default: false
      self_signed_signature_checks:
        description: Enable ad-hoc signature integrity checks (no secrets)
        type: boolean
        required: false
        default: false
      expected_team_id:
        description: Expected TeamIdentifier (optional, recommended)
        required: false
        default: ""
      require_notarization:
        description: Require notarization submit/staple/assessment
        type: boolean
        required: false
        default: false

permissions:
  contents: read

jobs:
  package-test:
    name: Package test on ${{ inputs.runner }}
    runs-on: ${{ inputs.runner }}
    timeout-minutes: 60

    steps:
      - name: Resolve binary asset
        id: resolve
        shell: bash
        run: |
          set -euo pipefail

          arch_input="${{ inputs.binary_arch }}"
          if [[ "$arch_input" == "auto" ]]; then
            case "$(uname -m)" in
              arm64|aarch64) arch="arm64" ;;
              x86_64|amd64) arch="amd64" ;;
              *)
                echo "Unsupported runner arch: $(uname -m)"
                exit 1
                ;;
            esac
          else
            arch="$arch_input"
          fi

          asset="distromate-darwin-${arch}"
          url="https://github.com/${{ github.repository }}/releases/download/${{ inputs.release_tag }}/${asset}"

          echo "arch=$arch" >> "$GITHUB_OUTPUT"
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
          echo "url=$url" >> "$GITHUB_OUTPUT"

          echo "Runner arch: $(uname -m)"
          echo "Binary arch: $arch"
          echo "Asset: $asset"

      - name: Download distromate binary
        shell: bash
        run: |
          set -euo pipefail
          curl -fL "${{ steps.resolve.outputs.url }}" -o distromate
          chmod +x distromate
          ./distromate --version

      - name: Prepare sample package project
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p sample-app

          if [[ "${{ inputs.production_signature_checks || inputs.self_signed_signature_checks }}" == "true" ]]; then
            cat > sample-app/smoke.c <<'EOF'
          #include <stdio.h>

          int main(void) {
            puts("Hello from signed app");
            return 0;
          }
          EOF

            clang sample-app/smoke.c -o sample-app/smoke
            rm -f sample-app/smoke.c
            chmod +x sample-app/smoke

            cat > sample-app/readme.txt <<'EOF'
          distromate package signature smoke test
          EOF

            cat > sample-app/distromate.yaml <<'EOF'
          appid: test-appid
          name: Distromate Smoke Test
          description: Verify macOS dmg packaging workflow
          package:
            name: smoke-app
            executable: smoke
          EOF
          else
            cat > sample-app/hello.sh <<'EOF'
          #!/usr/bin/env bash
          echo "Hello from packaged app"
          EOF
            chmod +x sample-app/hello.sh

            cat > sample-app/readme.txt <<'EOF'
          distromate package smoke test
          EOF

            cat > sample-app/distromate.yaml <<'EOF'
          appid: test-appid
          name: Distromate Smoke Test
          description: Verify macOS dmg packaging workflow
          package:
            name: smoke-app
            executable: hello.sh
          EOF
          fi

          ls -la sample-app

      - name: Validate signing secrets
        if: ${{ inputs.production_signature_checks }}
        shell: bash
        env:
          MACOS_CODESIGN_P12_BASE64: ${{ secrets.MACOS_CODESIGN_P12_BASE64 }}
          MACOS_CODESIGN_P12_PASSWORD: ${{ secrets.MACOS_CODESIGN_P12_PASSWORD }}
          MACOS_CODESIGN_IDENTITY: ${{ secrets.MACOS_CODESIGN_IDENTITY }}
        run: |
          set -euo pipefail

          missing=0
          for key in MACOS_CODESIGN_P12_BASE64 MACOS_CODESIGN_P12_PASSWORD MACOS_CODESIGN_IDENTITY; do
            if [[ -z "${!key:-}" ]]; then
              echo "::error::Missing required secret: $key"
              missing=1
            fi
          done

          if [[ "$missing" -ne 0 ]]; then
            exit 1
          fi

      - name: Import signing certificate
        if: ${{ inputs.production_signature_checks }}
        shell: bash
        env:
          MACOS_CODESIGN_P12_BASE64: ${{ secrets.MACOS_CODESIGN_P12_BASE64 }}
          MACOS_CODESIGN_P12_PASSWORD: ${{ secrets.MACOS_CODESIGN_P12_PASSWORD }}
        run: |
          set -euo pipefail

          cert_path="$RUNNER_TEMP/macos_codesign.p12"
          keychain_path="$RUNNER_TEMP/macos-build.keychain-db"
          keychain_password="$(openssl rand -hex 20)"

          echo "$MACOS_CODESIGN_P12_BASE64" | base64 --decode > "$cert_path"

          security create-keychain -p "$keychain_password" "$keychain_path"
          security set-keychain-settings -lut 21600 "$keychain_path"
          security unlock-keychain -p "$keychain_password" "$keychain_path"

          security list-keychains -d user -s "$keychain_path"

          security import "$cert_path" -k "$keychain_path" -P "$MACOS_CODESIGN_P12_PASSWORD" -A -T /usr/bin/codesign -T /usr/bin/security
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "$keychain_password" "$keychain_path"

          security find-identity -v -p codesigning "$keychain_path"

      - name: Verify distromate release signature trust
        if: ${{ inputs.production_signature_checks }}
        shell: bash
        run: |
          set -euo pipefail

          target="./distromate"
          expected_team="${{ inputs.expected_team_id }}"

          codesign --verify --strict --verbose=4 "$target"
          sign_info="$(codesign -dv --verbose=4 "$target" 2>&1)"
          printf '%s\n' "$sign_info"

          if ! printf '%s\n' "$sign_info" | grep -q '^Authority=Developer ID Application:'; then
            echo "::error::distromate is not signed with Developer ID Application certificate"
            exit 1
          fi

          team_line="$(printf '%s\n' "$sign_info" | grep '^TeamIdentifier=' | head -n1 || true)"
          team_id="${team_line#TeamIdentifier=}"
          if [[ -z "$team_id" || "$team_id" == "not set" ]]; then
            echo "::error::distromate TeamIdentifier missing (ad-hoc signature is not allowed)"
            exit 1
          fi

          if [[ -n "$expected_team" && "$team_id" != "$expected_team" ]]; then
            echo "::error::distromate TeamIdentifier mismatch: expected=$expected_team actual=$team_id"
            exit 1
          fi

          spctl --assess --type execute --verbose=4 "$target"

      - name: Sign sample executable with Developer ID
        if: ${{ inputs.production_signature_checks }}
        shell: bash
        env:
          MACOS_CODESIGN_IDENTITY: ${{ secrets.MACOS_CODESIGN_IDENTITY }}
        run: |
          set -euo pipefail

          binary_path="sample-app/smoke"
          codesign --force --options runtime --timestamp --sign "$MACOS_CODESIGN_IDENTITY" "$binary_path"
          codesign --verify --strict --verbose=4 "$binary_path"
          codesign -dv --verbose=4 "$binary_path" 2>&1

      - name: Sign sample executable with ad-hoc signature
        if: ${{ inputs.self_signed_signature_checks && !inputs.production_signature_checks }}
        shell: bash
        run: |
          set -euo pipefail

          binary_path="sample-app/smoke"
          codesign --force --sign - "$binary_path"
          codesign --verify --strict --verbose=4 "$binary_path"

          app_sign_info="$(codesign -dv --verbose=4 "$binary_path" 2>&1)"
          printf '%s\n' "$app_sign_info"

          if ! printf '%s\n' "$app_sign_info" | grep -q '^Signature=adhoc'; then
            echo "::error::Sample executable is not ad-hoc signed"
            exit 1
          fi

      - name: Run package command
        shell: bash
        run: |
          set -euo pipefail
          pushd sample-app >/dev/null
          ../distromate package -v "${{ inputs.app_version }}" -o out --debug 2>&1 | tee package-command.log

          if [[ "${{ inputs.production_signature_checks || inputs.self_signed_signature_checks }}" == "true" ]]; then
            grep -q "更新后签名校验通过" package-command.log
            grep -q "launcher/updater 全量DMG自动更新、拉起与签名校验测试通过" package-command.log
          fi

          popd >/dev/null

      - name: Sign generated DMG with ad-hoc signature
        if: ${{ inputs.self_signed_signature_checks && !inputs.production_signature_checks }}
        shell: bash
        run: |
          set -euo pipefail

          dmg="sample-app/out/smoke-app-Setup-${{ inputs.app_version }}.dmg"
          test -f "$dmg"

          codesign --force --sign - "$dmg"
          codesign --verify --verbose=4 "$dmg"

          dmg_sign_info="$(codesign -dv --verbose=4 "$dmg" 2>&1)"
          printf '%s\n' "$dmg_sign_info"

          if ! printf '%s\n' "$dmg_sign_info" | grep -q '^Signature=adhoc'; then
            echo "::error::Generated DMG is not ad-hoc signed"
            exit 1
          fi

      - name: Validate notarization secrets
        if: ${{ inputs.production_signature_checks && inputs.require_notarization }}
        shell: bash
        env:
          MACOS_NOTARY_APPLE_ID: ${{ secrets.MACOS_NOTARY_APPLE_ID }}
          MACOS_NOTARY_TEAM_ID: ${{ secrets.MACOS_NOTARY_TEAM_ID }}
          MACOS_NOTARY_APP_PASSWORD: ${{ secrets.MACOS_NOTARY_APP_PASSWORD }}
        run: |
          set -euo pipefail

          missing=0
          for key in MACOS_NOTARY_APPLE_ID MACOS_NOTARY_TEAM_ID MACOS_NOTARY_APP_PASSWORD; do
            if [[ -z "${!key:-}" ]]; then
              echo "::error::Missing required secret: $key"
              missing=1
            fi
          done

          if [[ "$missing" -ne 0 ]]; then
            exit 1
          fi

      - name: Notarize and staple generated DMG
        if: ${{ inputs.production_signature_checks && inputs.require_notarization }}
        shell: bash
        env:
          MACOS_NOTARY_APPLE_ID: ${{ secrets.MACOS_NOTARY_APPLE_ID }}
          MACOS_NOTARY_TEAM_ID: ${{ secrets.MACOS_NOTARY_TEAM_ID }}
          MACOS_NOTARY_APP_PASSWORD: ${{ secrets.MACOS_NOTARY_APP_PASSWORD }}
        run: |
          set -euo pipefail

          dmg="sample-app/out/smoke-app-Setup-${{ inputs.app_version }}.dmg"
          test -f "$dmg"

          xcrun notarytool submit "$dmg" \
            --apple-id "$MACOS_NOTARY_APPLE_ID" \
            --team-id "$MACOS_NOTARY_TEAM_ID" \
            --password "$MACOS_NOTARY_APP_PASSWORD" \
            --wait

          xcrun stapler staple "$dmg"
          xcrun stapler validate "$dmg"

      - name: Verify DMG content
        shell: bash
        run: |
          set -euo pipefail

          dmg="sample-app/out/smoke-app-Setup-${{ inputs.app_version }}.dmg"
          test -f "$dmg"

          hdiutil verify "$dmg"

          mount_dir="$(mktemp -d)"
          trap 'hdiutil detach "$mount_dir" >/dev/null 2>&1 || true; rm -rf "$mount_dir"' EXIT

          hdiutil attach "$dmg" -nobrowse -mountpoint "$mount_dir"
          ls -la "$mount_dir"

          test -L "$mount_dir/Applications"

          if [[ "${{ inputs.production_signature_checks || inputs.self_signed_signature_checks }}" == "true" ]]; then
            test -f "$mount_dir/smoke"
            run_output="$("$mount_dir/smoke")"
            echo "Mounted executable output: $run_output"
            test "$run_output" = "Hello from signed app"
          else
            test -f "$mount_dir/hello.sh"
            test -f "$mount_dir/readme.txt"
            run_output="$("$mount_dir/hello.sh")"
            echo "Mounted executable output: $run_output"
            test "$run_output" = "Hello from packaged app"
          fi

          echo "DMG check passed: $dmg"

      - name: Verify production signature trust on packaged executable
        if: ${{ inputs.production_signature_checks }}
        shell: bash
        run: |
          set -euo pipefail

          dmg="sample-app/out/smoke-app-Setup-${{ inputs.app_version }}.dmg"
          expected_team="${{ inputs.expected_team_id }}"

          mount_dir="$(mktemp -d)"
          trap 'hdiutil detach "$mount_dir" >/dev/null 2>&1 || true; rm -rf "$mount_dir"' EXIT

          hdiutil attach "$dmg" -nobrowse -mountpoint "$mount_dir"

          bin_path="$mount_dir/smoke"
          codesign --verify --strict --verbose=4 "$bin_path"

          sign_info="$(codesign -dv --verbose=4 "$bin_path" 2>&1)"
          printf '%s\n' "$sign_info"

          if ! printf '%s\n' "$sign_info" | grep -q '^Authority=Developer ID Application:'; then
            echo "::error::Packaged executable is not signed with Developer ID Application certificate"
            exit 1
          fi

          team_line="$(printf '%s\n' "$sign_info" | grep '^TeamIdentifier=' | head -n1 || true)"
          team_id="${team_line#TeamIdentifier=}"
          if [[ -z "$team_id" || "$team_id" == "not set" ]]; then
            echo "::error::Packaged executable TeamIdentifier missing (ad-hoc signature is not allowed)"
            exit 1
          fi

          if [[ -n "$expected_team" && "$team_id" != "$expected_team" ]]; then
            echo "::error::Packaged executable TeamIdentifier mismatch: expected=$expected_team actual=$team_id"
            exit 1
          fi

          spctl --assess --type execute --verbose=4 "$bin_path"

          if [[ "${{ inputs.require_notarization }}" == "true" ]]; then
            xcrun stapler validate "$dmg"
            spctl --assess --type open --verbose=4 "$dmg"
          else
            echo "Skip notarization-required checks (require_notarization=false)."
          fi

          echo "Production signature trust checks passed"

      - name: Verify self-signed signature integrity on packaged assets
        if: ${{ inputs.self_signed_signature_checks && !inputs.production_signature_checks }}
        shell: bash
        run: |
          set -euo pipefail

          dmg="sample-app/out/smoke-app-Setup-${{ inputs.app_version }}.dmg"
          codesign --verify --verbose=4 "$dmg"

          dmg_sign_info="$(codesign -dv --verbose=4 "$dmg" 2>&1)"
          printf '%s\n' "$dmg_sign_info"
          if ! printf '%s\n' "$dmg_sign_info" | grep -q '^Signature=adhoc'; then
            echo "::error::Packaged DMG signature is not ad-hoc"
            exit 1
          fi

          mount_dir="$(mktemp -d)"
          trap 'hdiutil detach "$mount_dir" >/dev/null 2>&1 || true; rm -rf "$mount_dir"' EXIT

          hdiutil attach "$dmg" -nobrowse -mountpoint "$mount_dir"

          bin_path="$mount_dir/smoke"
          codesign --verify --strict --verbose=4 "$bin_path"

          bin_sign_info="$(codesign -dv --verbose=4 "$bin_path" 2>&1)"
          printf '%s\n' "$bin_sign_info"
          if ! printf '%s\n' "$bin_sign_info" | grep -q '^Signature=adhoc'; then
            echo "::error::Packaged executable signature is not ad-hoc"
            exit 1
          fi

          echo "Self-signed signature integrity checks passed"

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: smoke-dmg-${{ inputs.runner }}-${{ steps.resolve.outputs.arch }}
          path: sample-app/out/*.dmg
          if-no-files-found: error

      - name: Start tmate session for debug
        if: ${{ failure() && inputs.debug_enabled }}
        uses: mxschmitt/action-tmate@v3
        with:
          limit-access-to-actor: true
